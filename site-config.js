// Central site configuration
window.SiteConfig = window.SiteConfig || {
  // defaultTheme: 'dark' or 'light'
  defaultTheme: 'dark'
};

// You can update this file to change defaults across all pages.
